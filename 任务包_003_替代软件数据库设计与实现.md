# Windows Clean Agent
# Task 003：替代软件数据库设计与实现任务包

版本：V3.0 Phase-2

## 一、任务目标

在 Task 002“软件资产扫描 + 软件指纹识别”基础上，实现软件功能替代数据库，让 Agent 理解一个软件不是一个名字，而是一项功能。

## 二、严格限制

本阶段只建立数据库并输出推荐方案。禁止自动安装软件、自动卸载软件、删除文件、修改默认程序或修改注册表。

用户追加约束：浏览器是保护项，不生成浏览器替代或迁移建议，不修改任何浏览器、收藏夹或默认浏览器设置。

## 三、目录与数据结构

新增：

```text
database/
├── replacement_database.json
├── software_category.json
└── verification_rules.json
```

每个替代条目必须包含原软件 `name`、`category`、`function` 和 `replacement`。每个替代方案包含软件名称、类型、官网、验证方式和 `high`/`medium`/`low` 可信度。

## 四、首批规则

- 图片查看：2345看图王 → ImageGlass、IrfanView
- 压缩：360压缩 → 7-Zip；2345好压 → 7-Zip、Bandizip
- 安全：360安全卫士、360杀毒 → Windows Defender
- 驱动：360驱动大师、驱动人生、驱动精灵 → Windows Update、设备厂商官方工具
- 下载：迅雷 → 浏览器内置下载、Motrix、aria2
- 浏览器：按用户追加约束保护并跳过，不提供替代建议

## 五、代码要求

新增 `src/replacement/database.py`、`loader.py` 和 `matcher.py`，读取三套数据库，以 `software_profile.json` 为输入并生成 `replacement_suggestion.json`。

## 六、测试和验收

在 `tests/replacement/` 测试 2345看图王、360压缩、360安全卫士等必需规则，并测试浏览器保护边界。

运行：

```text
python main.py analyze
```

必须能读取软件画像、匹配替代方案并输出推荐；不得执行安装、卸载、删除或系统修改。

提交信息：`feat: add replacement database`

# 任务结束
