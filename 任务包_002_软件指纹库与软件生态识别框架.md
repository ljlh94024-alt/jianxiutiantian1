# Windows Clean Agent
# Task 002：软件指纹库与软件生态识别框架开发任务包

版本：V3.0 Phase-1

## 目标与边界

在已有 Windows 软件资产扫描基础上，实现软件家族识别、软件功能分类、软件风险初步标记和软件生态画像生成。

本阶段只分析，禁止执行删除软件、卸载软件、修改注册表、修改启动项或修改系统配置。

## 一、背景

项目定位为 Windows 家庭电脑软件生态整理 Agent。最终流程是发现软件生态、理解软件用途、寻找替代方案、迁移功能，最后才删除冗余组件。当前阶段只完成“理解电脑”。

## 二、软件指纹数据库

新增 `rules/software_fingerprint/`，包含 `360.yaml`、`2345.yaml`、`tencent.yaml`、`baidu.yaml`、`sogou.yaml` 和 `xunlei.yaml`。

规则使用 YAML，包含 `family`、按名称/发布商/路径/文件划分的 `keywords`、`category` 与 `risk_level`。规则必须可扩展，以后新增软件只添加 YAML，不修改核心代码。

## 三、软件识别引擎

在 `src/software/` 建立 `fingerprint.py`、`classifier.py` 和 `models.py`。读取 `software_inventory.json`，匹配名称、发布商、安装路径、文件路径，并输出 `software_profile.json`。

画像字段包含：`name`、`family`、`category`、`risk_level` 和 `matched_by`。

## 四、分类系统

第一版分类包括：

- `security`：安全软件
- `browser`：浏览器
- `archive`：压缩软件
- `image_viewer`：看图软件
- `driver_tool`：驱动工具
- `downloader`：下载工具
- `input_method`：输入法

## 五、第一批支持的软件

- 360：360安全卫士、360杀毒、360浏览器、360压缩、360驱动大师；关键词 `360`、`Qihoo`、`QH`、`360safe`
- 2345：2345浏览器、2345好压、2345看图王；关键词 `2345`、`Haozip`、`Kuaizip`
- 腾讯：腾讯电脑管家、QQ浏览器、腾讯软件中心；关键词 `Tencent`、`QQ`
- 百度：关键词 `Baidu`
- 搜狗：关键词 `Sogou`
- 迅雷：关键词 `Xunlei`、`Thunder`

## 六、风险等级

风险等级只做标记，不执行操作：

- `S0`：普通软件，例如 Chrome、Office
- `S1`：可能长期不用
- `S2`：推广型软件，例如 2345 组件、部分全家桶组件
- `S3`：高权限软件，例如驱动工具、安全软件，需要未来人工确认

## 七、测试与验收

在 `tests/software/` 增加测试，至少验证：

- `2345看图王` → family `2345`、category `image_viewer`
- `360安全卫士` → family `360`、category `security`

运行 `python main.py scan` 可以生成 `software_inventory.json` 和 `software_profile.json`，并正确识别 360、2345、腾讯、百度、搜狗和迅雷组件。不得出现删除、卸载或系统修改动作。

## 八、提交与交付

新增规则、识别模块和测试，更新 README 或开发文档。提交信息格式：

```text
feat: add software fingerprint engine
```

提交后报告修改文件列表、实现功能、测试结果、已知问题和下一步建议。

任务结束。
