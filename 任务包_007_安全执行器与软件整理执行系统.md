# Windows Clean Agent
# Task 007：安全执行器与软件整理执行系统

版本：V3.4

## 任务目标

在 Task005/006 的授权 Agent、任务队列和网页确认基础上，实现固定替代软件包执行与客户电脑流氓组件整理执行器。AI 不直接执行；所有任务必须来自网页人工确认、白名单动作、Agent 权限检查和目标用户授权。

## 固定替代包

- 看图：ImageGlass，目标为 2345看图王、360看图组件，验证 JPG/PNG/WEBP
- 压缩：7-Zip，目标为 360压缩、2345好压、快压，验证 ZIP/7z/RAR
- 播放器：VLC，目标为广告播放器、万能播放器，验证 MP4/MKV

安装包元数据位于 `packages/viewer/`、`packages/archive/`、`packages/player/`。必须校验名称、版本、SHA-256、来源、静默参数、验证项目；安装包含浏览器、输入法、安全软件、桌面助手或广告组件时立即失败。仓库中的零哈希表示包尚未部署，禁止自动下载。

## 允许动作

仅允许 `install_replacement`、`uninstall_component`、`optimize_input_method`。卸载必须命中精确目标白名单：360压缩、360安全组件、360驱动工具、2345看图王、2345好压、2345安全组件、驱动人生、驱动精灵、快压。禁止批量删除、浏览器卸载、WPS/Office/LibreOffice 卸载、远程 Shell、禁用安全、绕过权限和隐藏执行。

## 输入法规则

输入法主体、词库、使用习惯和默认输入法保持不动。`optimize_input_method` 只处理推广启动项、推广服务、通知入口和推广缓存；所有实际修改必须通过显式注入的 CleanupBackend。

## 执行与验证

安全门要求目标身份、人工确认、Task005 权限和白名单动作。默认 dry-run；真实 ProcessRunner、UninstallRunner、CleanupBackend、VerificationBackend 必须由目标电脑的受审计部署显式注入。失败状态为 `failed`，上传错误原因并停止后续动作。

## 交付与验收

测试覆盖固定包哈希和捆绑检测、安装/卸载白名单、WPS/浏览器/输入法主体保护、输入法推广整理、验证失败停止、Task005 授权握手和禁止动作。提交信息：`feat: add safe software execution engine`。

# 任务结束
