# Windows Clean Agent
# Task 004：目标电脑指定软件生态整理规划器与维护模式设计任务包

版本：V3.1（收紧范围修订版）

## 一、项目定位

开发电脑是 Kilo Code + AI 开发环境，绝不能修改。目标家庭电脑只接受分析和规划。本项目不是“国产软件清理器”，而是家庭电脑高风险软件生态整理工具。

## 二、任务目标与禁令

读取目标画像、软件画像与替代建议，经保护名单过滤和目标生态判断，生成 `reports/migration_plan.json` 与 `reports/migration_report.json`。禁止删除、卸载、安装、修改注册表、启动项、默认程序或系统配置。

## 三、目标身份

目标画像包含 `target_id`、`os`、`user_type`、`scan_time`。扫描画像、替代建议及规划目标必须携带相同 `target_id`，否则拒绝规划。

## 四、保护等级

- P0：系统保护，`ignore`
- P1：用户软件保护，`ignore`
- P2：正常软件，`report_only`
- P3：可评估软件，`review`
- P4：高推广目标软件，可 `recommend`，但必须人工确认

任何层级都禁止 `delete`、`uninstall`、`force_remove`。

## 五、必须保护

办公软件、通讯协作软件、浏览器、输入法及专业生产力软件默认 P1。包括 WPS、Office、LibreOffice、微信、QQ、企业微信、钉钉、飞书、搜狗/百度输入法、Photoshop、CAD、Lightroom。浏览器缺少“非主浏览器、明显不用、用户确认”证据时必须保护。

## 六、目标范围

P4 仅包括明确列出的 360/2345 高推广组件；360/2345 浏览器同时命中保护规则时 P1 优先。驱动人生、驱动精灵、鲁大师、快压为 P3，必须人工审查。其他软件均为 P2，仅报告。

## 七、实现与验收

保护规则位于 `rules/software_protection/`。规划模块位于 `src/migration/`。`src/repair/` 只预留 R0-R3 检测、计划、验证接口，不提供执行器。

测试覆盖 ImageGlass、7-Zip、浏览器/办公/通讯/输入法/专业软件保护、P3 审查、目标身份校验及禁止动作。

提交信息：`feat: add software migration planner`

# 任务结束
